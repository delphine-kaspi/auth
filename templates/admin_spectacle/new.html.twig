{% extends 'base.html.twig' %}

{% block title %}Nouveau spectacle{% endblock %}

{% block body %}

    <div class="container my-5">
        <div class="bg-white col-md-6 mx-auto p-4 border border-2 border-green rounded shadow">

            <h2 class="text-center mb-4">Créer un nouveau spectacle</h2>

            {{ include('admin_spectacle/_form.html.twig') }}

            <div class="text-center mt-3">
                <a href="{{ path('app_admin_spectacle_index', {'id': artiste.id}) }}" class="btn btn-secondary">
                    Retour à la liste
                </a>
            </div>

        </div>
    </div>

{% endblock %}